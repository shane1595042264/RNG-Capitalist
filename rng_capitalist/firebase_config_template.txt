// FIREBASE CONFIGURATION TEMPLATE
// Replace the values below with your actual Firebase configuration

// After you create your Firebase project and add a Flutter app, 
// you'll get a configuration object that looks like this:

/*
const firebaseConfig = {
  apiKey: "your-api-key-here",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "your-sender-id",
  appId: "your-app-id"
};
*/

// Copy the values from your Firebase Console and update firebase_options.dart
// Here's what you need to replace:

// 1. YOUR_PROJECT_ID → your actual project ID
// 2. YOUR_WEB_API_KEY → your web API key
// 3. YOUR_WEB_APP_ID → your web app ID
// 4. YOUR_SENDER_ID → your messaging sender ID
// 5. YOUR_MEASUREMENT_ID → your measurement ID (optional)

// Example:
// If your project ID is "rng-capitalist-abc123"
// Then replace "YOUR_PROJECT_ID" with "rng-capitalist-abc123"

// The same values can be used for all platforms (Windows, Web, Android, iOS)
// Firebase will handle the platform-specific authentication automatically
